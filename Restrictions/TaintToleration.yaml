apiVersion: v1
kind: Pod
metadata:
  name: nginx
spec:
  containers:
  - name: nginx
    image: nginx
  tolerations:
  - key: "key"
    operator: "Equal"
    value: "value"
    effect: "NoSchedule"
# -----------------
# kubectl taint nodes minikube  key=value:NoSchedule
# kubectl taint nodes minikube key=value:NoSchedule-
# --
# kubectl taint nodes minikube key=value:NoExecute
# kubectl taint nodes minikube key=value:NoExecute-
